import React from "react";

function FAQScreen(props) {

    return (
        <div>
        <section class="faq">
            FAQ's
        </section>

        <footer class="footer">
            Footer
        </footer>
        </div>
    )
}

export default FAQScreen;